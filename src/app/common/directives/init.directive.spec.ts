import { InitDirective } from './init.directive';

describe('InitDirective', () => {
  it('should create an instance', () => {
    const directive = new InitDirective();
    expect(directive).toBeTruthy();
  });
});
